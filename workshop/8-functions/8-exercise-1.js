// Q1 ***
// Write a function that accepts two integers and displays
// the greater one, using `console.log`

// Define the function
function max(num1, num2) {
  let = greaternumber;
  if (num1 > num2) {
    console.log(num1);
  } else {
    console.log(num2);
  }
}
console.log((greaternumber = "The greater number"));

// Call the function
max(12, 43);
